use rust_embed::RustEmbed;

#[derive(RustEmbed)]
#[folder = "sql"]
struct Asset;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
